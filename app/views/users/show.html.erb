<h1>Hello <%= current_user.username %>, welcome to your profile page</h1>
<h2>Projects You Have Supported:</h2>
<% current_user.supported_projects.each do |project| %>
  <%= link_to project.name, project_url(project.id) %><br>
<% end %>

<h2>Project You Have Created:</h2>
<% current_user.created_projects.each do |project| %>
  <%= link_to project.name, project_url(project.id) %><br>
<% end %>